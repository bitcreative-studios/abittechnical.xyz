import constants from './constants'
const theme = {
  colors: constants.COLORS,
  fontFamily: constants.FONTS.fontFamily,
  fontSize: constants.FONTS.fontSize,
  boxShadow: constants.BOX_SHADOW,
}

export default theme
